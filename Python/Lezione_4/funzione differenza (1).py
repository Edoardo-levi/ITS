def divisione (a: int ,b: int) -> int :
    """
    a: dividendo
    b: divisore
    return divisione -> il risultato della divisione
    """
    
    divisione=int(a/b)
    return divisione
    
divisione=divisione (a=4,b=2)         # richiamo la funzione

print(divisione)